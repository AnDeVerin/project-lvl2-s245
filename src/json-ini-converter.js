// var fs = require('fs'), ini = require('ini');

// var config = JSON.parse(fs.readFileSync('./after.json', 'utf-8'));

// fs.writeFileSync('./after.ini', ini.stringify(config, { section: 'section' }));
